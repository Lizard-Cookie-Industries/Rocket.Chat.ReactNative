# Android
# Build your Android project with Gradle.
# Add steps that test, sign, and distribute the APK, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/android

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

name: $(date:yyyy).$(Month)$(rev:.r)

steps:

- script: yarn install

- task: DownloadSecureFile@1
  name: googleJSON
  displayName: 'Download Google Services config'
  inputs:
    secureFile: 'google-services.json'

- script: |
    echo Installing $(googleJSON.secureFilePath) to the android/app/ directory...
    sudo cp $(googleJSON.secureFilePath) $(Build.SourcesDirectory)/android/app/

- task: Gradle@2
  inputs:
    workingDirectory: 'android/'
    gradleWrapperFile: 'android/gradlew'
    gradleOptions: '-Xmx3072m'
    publishJUnitResults: false
    testResultsFiles: '**/TEST-*.xml'
    tasks: 'assembleDebug'
